apiVersion: v1
kind: Secret
metadata:
  name: suricata-command-auth
  namespace: security
type: Opaque
stringData:
  # Placeholder only. Operators should source this value from 1Password or
  # another secret manager and must not commit the real token.
  token: REPLACE_WITH_OP_MANAGED_SURICATA_COMMAND_TOKEN
