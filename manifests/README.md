# Kubernetes Manifests

This directory contains all Kubernetes manifests for deploying the router/firewall components.

## Directory Structure

- `core/` - Core system components (Cilium, cert-manager, etc.)
- `network/` - Network services (DNS, DHCP, routing)
- `security/` - Security services (Suricata, Zeek, VPN)