apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespaces.yaml
  # Sprint 31 / Ticket 49 — internal CA chain. Listed before any
  # consumers so cert-manager has the ClusterIssuers ready when
  # per-controller Certificate objects reconcile.
  - certificates
  - cilium
  - core
  - security
  - threatintel
  - trafficshaper
  - dns
  - dhcp
  - monitoring
  - vpn
  - frr